# Firebase Admin SDK (server-side only; never prefix these with NEXT_PUBLIC_)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Keep the value quoted. Both real newlines and escaped \\n sequences are supported.
FIREBASE_PRIVATE_KEY=""
